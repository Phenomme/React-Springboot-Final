spring.application.name=task-manager

# MySQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/task_java_spring
spring.datasource.username=michaeledwardsii
spring.datasource.password=phenomme5!
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate JPA settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Enable H2 console if you want to use it for viewing the data
spring.h2.console.enabled=false